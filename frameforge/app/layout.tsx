import './globals.css';
export const metadata={title:'Frameforge — Video Editor',description:'Cinematic video editing portfolio by Frameforge'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
