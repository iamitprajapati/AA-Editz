import { NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';
import { Resend } from 'resend';
export async function POST(req:Request){const form=await req.formData();const name=String(form.get('name')||'');const email=String(form.get('email')||'');const project_type=String(form.get('project_type')||'');const message=String(form.get('message')||'');if(!name||!email||!message)return NextResponse.json({error:'Missing fields'},{status:400});const admin=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);const {error}=await admin.from('inquiries').insert({name,email,project_type,message});if(error)return NextResponse.json({error:error.message},{status:500});if(process.env.RESEND_API_KEY){const resend=new Resend(process.env.RESEND_API_KEY);await resend.emails.send({from:'Frameforge <onboarding@resend.dev>',to:[process.env.NOTIFICATION_EMAIL!],subject:`New Frameforge inquiry from ${name}`,html:`<h2>New project inquiry</h2><p><b>Name:</b> ${name}</p><p><b>Email:</b> ${email}</p><p><b>Project:</b> ${project_type}</p><p>${message.replace(/\n/g,'<br>')}</p>`});}return NextResponse.redirect(new URL('/?sent=1#contact',req.url));}
